# Backend API Flows Tasks

## 1. Implement Users API
**What**: Create logic in `UsersController` and `UsersService`.
**Where**: `src/modules/users/`
**Done when**: `POST /users`, `GET /users/:id`, and `GET /users` are implemented.

## 2. Implement Vehicles API
**What**: Create logic in `VehiclesController` and `VehiclesService`.
**Where**: `src/modules/vehicles/`
**Done when**: `POST /vehicles` and `GET /vehicles` are implemented.

## 3. Implement Availability API
**What**: Create logic in `AvailabilityController` and `AvailabilityService`.
**Where**: `src/modules/availability/`
**Done when**: `POST /availability` and `GET /availability` are implemented.

## 4. Implement Lessons API
**What**: Create logic in `LessonsController` and `LessonsService`.
**Where**: `src/modules/lessons/`
**Done when**: `POST /lessons`, `GET /lessons`, and `PATCH /lessons/:id` are implemented.

## 5. Implement Payments API
**What**: Create logic in `PaymentsController` and `PaymentsService`.
**Where**: `src/modules/payments/`
**Done when**: `POST /payments` and `GET /payments` are implemented.